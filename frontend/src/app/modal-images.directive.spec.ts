import { ModalImagesDirective } from './modal-images.directive';

describe('ModalImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalImagesDirective();
    expect(directive).toBeTruthy();
  });
});
